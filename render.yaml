services:
  - type: web
    name: aegis
    runtime: node
    buildCommand: ""
    startCommand: npm start
    healthCheckPath: /api/ready
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: NODE_ENV
        value: production
      - key: TRUST_PROXY
        value: "true"
